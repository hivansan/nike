<%= form_for(@commercial) do |f| %>
  <% if @commercial.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@commercial.errors.count, "error") %> prohibited this commercial from being saved:</h2>

      <ul>
      <% @commercial.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image %><br />
    <%= f.file_field :image %>
  </div>
  <div class="field">
    <%= f.label :video %><br />
    <%= f.file_field :video %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
