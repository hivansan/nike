<h1>Productos de <%= @products.first.category.name %> </h1>

<table class="table">
  <tr>
    <th>Nombre</th>
    <th>Categoría</th>
    <th>Descripci&oacute;n</th>
    <th>Imagen</th>
    <th>Video</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @products.each do |product| %>
  <tr>
    <td><%= product.name %></td>
    <td><%= product.category.id %></td>
    <td><%= product.description %></td>
    <td><%= product.image %></td>
    <td><%= product.video %></td>
    <td><%= link_to 'Show', category_product_path(@category, product) %></td>
    <td><%= link_to 'Edit', edit_category_product_path(@category, product) %></td>
    <td><%= link_to 'Destroy', category_product_path(product.category, product), method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nuevo Producto', new_category_product_path(@category) %>
